<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Your Figure</title>
  </head>
  <body>

<form action="/figures/<%=@figure.id%>" method="POST">
<input id="hidden" type="hidden" name="_method" value="PATCH">

  <label>Name:</label>
  <input type="text" name="figure[name]" id="figure_name" value="<%=@figure.name%>"></input>

  <label>Update Existing Landmarks:</label>
  <br></br>
  <%Landmark.all.each do |landmark|%>
    <input type="checkbox" name="figure[landmark_ids][]" id="<%=landmark.name%>" value="<%=landmark.id%>" <%='checked' if (@figure.landmark_ids.include? landmark.id) %>><%=landmark.name%></input>
  <%end%>
  <br></br>
  <label>and/or, create a new landmark:</label>
  <label>Landmark Name:</label>
    <input  type="text" name="landmark[name]" id="new_landmark"></input>
  <br></br>
  <label>Update Titles:</label>
  <br></br>
  <%Title.all.each do |title|%>
    <input type="checkbox" name="figure[title_ids][]" id="<%=title.name%>" value="<%=title.id%>" <%='checked' if (@figure.title_ids.include? title.id) %>><%=title.name%></input>
  <%end%>
  <label>and/or, create a new title:</label>
  <label>Title Name:</label>
    <input  type="text" name="figure[title_ids][]" id="new_title"></input>
  <br></br>
  <input type="submit" value="Edit Figure">
</form>
</body>
</html>
